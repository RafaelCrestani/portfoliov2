# Plano — paridade total do botão de currículo

1. Criar `.action-pill--primary` em `css/base.css` com o visual preto compartilhado.
2. Aplicar a variante ao e-mail e às duas instâncias do currículo em `index.html`.
3. Remover altura, padding e gap compactos de `.experience__resume-link--rail`.
4. Expandir o wrapper lateral em 16 px por lado para o componente completo caber em 961 px.
5. Remover a definição visual contextual duplicada do botão principal em `css/footer.css`.
6. Incrementar as versões de cache dos três estilos alterados.
7. Comparar propriedades computadas de currículo e e-mail em oito viewports.
8. Validar hover, foco, PDF, rodapé, overflow, truncamento e distância do contador.
9. Fazer commit, push, acompanhar o GitHub Pages e conferir o endereço público.
